import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Basic configuration for Shopify Next.js app on Cloudflare Workers
  // This enables the app to run on Cloudflare's edge network
  // with optimal performance for e-commerce workloads
});
